import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    artists: {
        marginTop: 10,
    },
    container: {
        width: 150,
        margin: 10,
    },
    image: {
        width: '100%',
        height: 150,
        borderRadius: 25,
    },
});

export default styles;